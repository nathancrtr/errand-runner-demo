# Copy to .env (git-ignored). errand loads it itself. No API keys: every source is public.
HOME_LAT=<your latitude>     # from any map app, four decimals
HOME_LON=<your longitude>    # .env is git-ignored; these live nowhere else
